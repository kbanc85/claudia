# Phase 3: Skills audit and porting

**Status**: [ ] Not started
**Duration estimate**: 6-8 days
**Critical path**: Yes (porting blocks 6 and 7)
**Can parallelise with**: Phases 4 and 5 can start late in Phase 3. Tasks 3.1, 3.2, 3.3 (analytical) can start during Phase 2A.
**Depends on**: Analytical tasks depend on Phase 1. Porting tasks (3.4+) depend on Phase 2B (need memory working).

## Objective
Port 12 core Claudia skills into the Hermes skill format. Verify they work across frontier and local models.

## The 12 core skills
1. `claudia-draft` (email composition, highest usage)
2. `morning-brief` (daily orientation)
3. `inbox-check` (email monitoring)
4. `research` / `cross-reference-research` (information lookup)
5. `capture-meeting` (transcript processing)
6. `draft-reply` (email replies)
7. `commit-commands` (git operations)
8. `meeting-prep` (pre-call briefings)
9. `new-person` (relationship creation)
10. `send-followup` (post-meeting follow-ups)
11. `what-am-i-missing` (risk/gap detection)
12. `weekly-review` (guided reflection)

## Tasks

- [ ] **3.1 Claude Code dependency analysis** _(can start during Phase 2A)_
  - For each of the 12 core skills, answer:
    - [ ] Does it require Claude Code's built-in file system access? → map to Hermes `file_tools`
    - [ ] Does it require Claude Code's bash execution? → map to Hermes `terminal_tool`
    - [ ] Does it require MCP tools that only exist in Claudia's daemon? → map to new memory provider
    - [ ] Does it conflict with an existing Hermes tool?

- [ ] **3.2 Model compatibility test script** _(can start during Phase 2A)_
  - Write automated test that:
    - [ ] Takes a skill's prompt template
    - [ ] Sends it to 3+ model endpoints (Claude Sonnet via OpenRouter, GPT-4.1, Llama 3.3 70B via local Ollama)
    - [ ] Captures output
    - [ ] Flags: malformed responses, missed instructions, hallucinated tool calls, format failures
    - [ ] Generates a compatibility report
  - [ ] Run against all 12 core skills

- [ ] **3.3 Skill spreadsheet** _(can start during Phase 2A)_
  - Files touched: `docs/decisions/skill-audit.md` (new)
  - Columns: Skill | Claude Code dependency | Hermes equivalent | Model compat | Decision

- [ ] **3.4 Port core skills**
  - Files touched: `~/.claudia/skills/claudia-core/`
  - For each of the 12:
    - [ ] Convert from Claudia's markdown template format to Hermes-style skill file
    - [ ] Follow agentskills.io standard: Level 0 name/description, Level 1 full skill, Level 2 reference files
    - [ ] Register via skill registry
    - [ ] Each gets a corresponding slash command

- [ ] **3.5 Archive non-core skills**
  - Files touched: `optional-skills/claudia-archive/`
  - [ ] Move remaining 29 skills
  - [ ] Users opt-in via `/skill toggle`

- [ ] **3.6 Self-improvement integration**
  - Files touched: `plugins/` or similar, skill generator logic
  - [ ] After complex tasks (5+ tool calls), agent generates a skill document
  - [ ] Apply Claudia's judgment filter before saving (does this align with chief-of-staff role?)
  - [ ] Saved skills go to `~/.claudia/skills/autogenerated/`

- [ ] **3.7 Subagent personality inheritance**
  - Recommendation (from roadmap): abbreviated Claudia persona (~500 tokens) covering voice + approval rules, no full relationship context or judgment layer.
  - Files touched: `tools/delegate_tool.py` or equivalent delegation entry point
  - [ ] Define abbreviated persona template
  - [ ] Inject when `delegate_task` is called
  - [ ] Verify gateway responses from subagents sound like Claudia

## Deliverable
12 core skills working across models, self-improvement loop respects Claudia judgment, subagent personality defined.

## Rollback
Keep only the 5 highest-usage skills: `claudia-draft`, `morning-brief`, `inbox-check`, `research`, `meeting-prep`. Archive the rest. Users can manually re-enable from archive.

## Decisions made this phase
- _none yet_ — expected: **Subagent personality inheritance** ADR (abbreviated persona template, what's included, what's dropped).

## Session handoff
_Last updated: 2026-04-08 by scaffold setup_
- **Last completed**: nothing — Phase 3 not yet started.
- **Next up**: Tasks 3.1-3.3 can begin in a parallel session while Phase 2A is in progress.
- **Blockers**: 3.4+ blocked on Phase 2B complete.
- **Notes**: Model compatibility script (3.2) produces artefacts that feed directly into 3.3 and eventually into Phase 4.4 testing. Save its output in `../data/skill-compat-reports/`.
